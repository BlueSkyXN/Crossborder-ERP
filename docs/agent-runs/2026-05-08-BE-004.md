# BE-004 仓库和基础配置

输入来自 `docs/ai-dev-baseline/agent-execution/tasks/BE-004.md`，并沿用当前约束：暂不使用 Docker，不安装或启动 PostgreSQL/MySQL/Redis，真实验证仅覆盖项目本地 SQLite。Agent 在 `ai/be004-warehouse-config` 分支完成 `warehouses` Django app，新增仓库、仓库地址、渠道、费率方案、包装方式和增值服务模型，提供用户端仓库列表/专属收货地址接口，以及后台仓库、渠道、包装、增值服务、费率配置 CRUD 接口。

关键取舍：复杂首重续重、体积重、偏远费、保险费先写入 `rule_json.TODO_CONFIRM`，不提前固化业务公式；停用渠道通过 `get_active_shipping_channels()` 作为后续运单选择入口，当前 BE-004 不实现运单模块。种子数据已补充深圳仓、北京仓、测试空运、测试海运、纸箱、编织袋、拍照、加固、防水。

验证结果：已执行 `uv run python manage.py check`、`uv run python manage.py spectacular --file /tmp/crossborder-openapi-be004.yaml`、`uv run pytest apps/warehouses apps/members apps/iam apps/common`、`uv run pytest`、`uv run python manage.py migrate --noinput`、`uv run python manage.py seed_demo`，均通过。未验证边界：PostgreSQL/MySQL/Redis/Celery 仍为后续配置兼容目标，本轮未做真实环境验证。
