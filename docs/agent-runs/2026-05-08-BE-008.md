# 2026-05-08 BE-008

输入来自 `docs/ai-dev-baseline/agent-execution/tasks/BE-008.md`，目标是实现物流轨迹、后台人工发货/添加轨迹、用户查询轨迹和确认收货。继续遵守本阶段约束：不接真实物流 API 自动同步，不暴露其他用户私有运单轨迹，不启动 Docker、PostgreSQL/MySQL/Redis。

本轮在 `apps.waybills` 中新增 `TrackingEvent` 和 `TrackingEventSource`，补充迁移、serializer、service 和 API。用户端接口覆盖 `GET /api/v1/waybills/{id}/tracking-events`、`GET /api/v1/waybills/tracking?waybill_no=xxx`、`POST /api/v1/waybills/{id}/confirm-receipt`；后台接口覆盖 `POST /api/v1/admin/waybills/{id}/ship`、`POST /api/v1/admin/waybills/{id}/tracking-events`。`ship_waybill` 只允许 `PENDING_SHIPMENT -> SHIPPED`，发货时创建首条轨迹并把关联 Parcel 推进到 `OUTBOUND`；`confirm_receipt` 只允许本人 `SHIPPED -> SIGNED` 并补签收轨迹。

验证已完成：`cd backend && uv run pytest apps/waybills`、`cd backend && uv run pytest`、`cd backend && uv run python manage.py check`、`cd backend && uv run python manage.py makemigrations --check --dry-run`、`cd backend && uv run python manage.py spectacular --file /tmp/crossborder-be008-openapi.yaml --validate` 均通过。测试覆盖后台发货创建轨迹、包裹出库、用户轨迹列表、按单号查询轨迹、确认收货、跨用户轨迹隔离和非法发货状态返回 `STATE_CONFLICT`。

未验证边界：真实物流 API 自动同步、批量发货、面单打印和前端轨迹面板不在 `BE-008` 范围，按任务图留给后续前端任务继续实现。下一步进入 `FEA-004`。
