# FEA-002 后台基础配置页面

输入来自 `docs/ai-dev-baseline/agent-execution/tasks/FEA-002.md`，基于 `FEA-001` 后台壳继续实现仓库配置工作台。本轮未引入 Docker、数据库或 Redis，仍使用本地 Django dev server + SQLite 做真实联动验收。Agent 在 `ai/fea002-admin-config-pages` 分支将 `/warehouses` 从占位页替换为基础配置页面，覆盖仓库、发货渠道、包装方式、增值服务和费率方案五个 Tab。

关键取舍：后端 BE-004 暂未提供服务端筛选分页，因此页面先做前端筛选、Ant Design 表格分页、loading、empty、error；新增/编辑使用统一 Modal 表单；启停和删除均使用 `Popconfirm` 二次确认；费率方案只编辑 JSON object，不实现复杂价格引擎 UI。渠道、包装、增值服务和费率方案均复用同一套 API client 与状态更新逻辑。

验证结果：已执行 `pnpm --filter @crossborder-erp/admin-web lint`、`pnpm --filter @crossborder-erp/admin-web build`、`pnpm lint`、`pnpm build`、`actionlint .github/workflows/ci.yml`，均通过。浏览器联动验收覆盖：admin 登录后进入 `/warehouses`，显示五类配置列表；通过页面新增 `QA250508` 验收仓；用户端 `GET /api/v1/warehouses/{id}/address` 可返回带 `CB000001` 的专属地址；通过二次确认停用后再启用该仓库；390px 移动视口无明显遮挡。
