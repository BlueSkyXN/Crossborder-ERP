# 2026-05-08 FEA-003

输入来自 `docs/ai-dev-baseline/agent-execution/tasks/FEA-003.md`，目标是补齐后台包裹入库高频页面：待入库列表、扫描入库、包裹详情、在库列表、无主包裹登记，以及 loading、empty、error、状态标签。人工约束保持不变：不使用 Docker，不启动或安装 PostgreSQL/MySQL/Redis，只在项目本地 SQLite 和本仓库依赖内验证。

本轮新增 `admin-web/src/features/parcels/`，接入 `/admin/parcels`、`/admin/parcels/scan-inbound`、`/admin/parcels/{id}/inbound`、`/admin/unclaimed-parcels`。后台页面采用 Ant Design 表格、Tabs、Drawer 和表单：待入库/在库按状态过滤，扫描页支持大单号输入、仓库、重量、长宽高、图片凭证和备注；详情抽屉展示基础信息、商品明细和入库凭证。后端入库请求补充可选 `photo_file_ids`，真实写入 `ParcelPhoto`，避免前端只做展示假字段。

验证已完成：`pnpm --filter @crossborder-erp/admin-web lint`、`pnpm --filter @crossborder-erp/admin-web build`、`pnpm lint`、`pnpm build`、`actionlint .github/workflows/ci.yml`、`cd backend && uv run pytest` 均通过。联动验收使用本地 dev server 和 SQLite：用户提交预报 `FEA0031778244865` 后，后台扫描入库成功，用户端 `GET /api/v1/parcels/packable` 回读为 `IN_STOCK`，并包含 `fea003-photo-001` 入库图片凭证。

未验证边界：未做 PDA、打印机、批量导入、真实文件上传、PostgreSQL/MySQL/Redis 验证。下一步进入 `FEU-001B`，在用户 Web 补齐包裹预报、列表和详情。
