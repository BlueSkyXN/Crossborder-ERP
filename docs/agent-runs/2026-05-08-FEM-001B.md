# 2026-05-08 FEM-001B

输入来自 `docs/ai-dev-baseline/agent-execution/tasks/FEM-001B.md`，目标是补齐移动 H5 的包裹预报、包裹列表、详情卡和 `IN_STOCK` 申请打包入口。继续遵守本阶段约束：不使用 Docker，不安装或启动 PostgreSQL/MySQL/Redis，验证基于项目本地依赖、SQLite 和 GitHub Actions。

本轮新增 `mobile-h5/src/features/parcels/`、`ParcelForecastPage` 和 `ParcelListPage`，接入 `POST /parcels/forecast` 与 `GET /parcels`。寄件首页的“发布预报”和“申请打包”按钮改为真实路由入口；预报页覆盖仓库、快递单号、承运商、商品、数量、申报价值和备注；包裹页使用横向移动卡片列表、状态筛选、详情卡、图片凭证和在库包裹底部操作栏。

验证已完成：`pnpm --filter @crossborder-erp/mobile-h5 lint`、`pnpm --filter @crossborder-erp/mobile-h5 build`、`pnpm lint`、`pnpm build`、`actionlint .github/workflows/ci.yml`、`cd backend && uv run pytest` 均通过。浏览器联动验收使用 375px 移动视口：登录后从 `/ship` 跳转预报页，提交 `tracking_no = MOBFEM05081501` 后列表可见；通过后台扫描入库后，移动端刷新显示 `IN_STOCK`、重量 `1.111 kg`、体积 `20.00 x 12.00 x 8.00 cm`、图片凭证 `fem001b-photo-001`，并显示申请打包入口，页面元素未与底部导航重叠。

未验证边界：申请打包当前只是入口提示，真实 Waybill 创建留给后续 `BE-006/FEU-001C/FEM-001C`；未做复杂批量功能、真实文件上传、外部数据库或 Redis 验证。下一步进入 `BE-006`。
