# 2026-05-08 FEU-001B

输入来自 `docs/ai-dev-baseline/agent-execution/tasks/FEU-001B.md`，目标是补齐用户 Web 的包裹预报、包裹列表、详情和 `IN_STOCK` 申请打包入口。继续遵守本阶段约束：不使用 Docker，不安装或启动 PostgreSQL/MySQL/Redis，验证基于项目本地依赖、SQLite 和 GitHub Actions。

本轮新增 `user-web/src/features/parcels/` 和 `ParcelsPage`，接入 `POST /parcels/forecast`、`GET /parcels`、`GET /parcels/{id}`、`GET /parcels/packable` 的前端类型与 API。页面提供仓库选择、快递单号、承运商、商品名称、数量、申报价值和备注；列表支持状态筛选、关键字搜索、分页和空状态；详情展示快递单号、仓库、状态、重量体积、入库时间、商品明细和图片凭证。控制台新增包裹预报/列表入口。

验证已完成：`pnpm --filter @crossborder-erp/user-web lint`、`pnpm --filter @crossborder-erp/user-web build`、`pnpm lint`、`pnpm build`、`actionlint .github/workflows/ci.yml`、`cd backend && uv run pytest` 均通过。浏览器联动验收使用本地 dev server 和 SQLite：用户提交 `tracking_no = TEST123` 后列表和详情可见；通过后台扫描入库后，用户页刷新显示 `IN_STOCK`、重量 `2.345 kg`、体积 `30.00 x 20.00 x 10.00 cm`、图片凭证 `feu001b-photo-001`，并显示申请打包入口。

未验证边界：申请打包按钮当前是入口提示，真实 Waybill 创建留给后续运单模块；未做批量导入、复杂无主认领审核、真实文件上传和外部数据库/Redis 验证。下一步进入 `FEM-001B`。
