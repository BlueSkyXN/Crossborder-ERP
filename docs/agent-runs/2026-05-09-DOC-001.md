# 2026-05-09 DOC-001

## 任务

交付文档、部署说明、演示脚本。

## 交付

- 扩充根 README，补齐项目目标、技术栈、本地启动、环境变量、迁移、seed、测试账号、测试命令、演示流程和已知边界。
- 新增 `docs/deployment/README.md`，记录当前 no-Docker local-first 部署、Docker Compose 暂缓边界、staging 配置、文件存储和 FAQ。
- 新增 `docs/demo-script.md`，覆盖集运主链路、最小代购链路和移动 H5 快速验收步骤。
- 新增 `docs/known-issues-and-roadmap.md`，按问题、影响、当前临时处理、后续建议、是否阻塞 v0.1 的格式记录未完成项。
- 新增 `docs/delivery-completion-audit.md`，映射用户约束、P0 链路和证据文件。
- 更新 `docs/README.md` 和 `agent-execution/current-state.yaml`，将任务图状态标记为完成。

## 验证

- `npm run e2e`：1 passed
- `cd backend && uv run python manage.py check`
- `cd backend && uv run python manage.py makemigrations --check --dry-run`
- `cd backend && uv run pytest`：58 passed
- `pnpm lint`
- `pnpm build`
- `actionlint .github/workflows/ci.yml`
- `git diff --check`
- 文档边界扫描：未发现把 Docker、PostgreSQL/MySQL、Redis 写成已验证能力

## 边界

- 不新增 Docker Compose 文件，不执行 Docker 验证。
- PostgreSQL/MySQL/Redis/Celery 仍保持 `configured_unverified`。
- 不把真实支付、自动采购、对象存储、打印和物流 API 写成已完成能力。

## 下一步

正式任务图已完成；后续进入人工验收、P1 稳定化和生产化规划。
