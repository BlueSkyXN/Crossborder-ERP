# 2026-05-09 FEM-001C

## 任务

移动 H5 运单列表、追踪、确认收货。

## 交付

- 新增移动端 `/ship/packing` 申请打包页，支持在库包裹选择、同仓校验、收件信息录入和 Waybill 创建。
- 新增移动端 `/ship/waybills` 运单中心，覆盖状态 Tab、运单卡片、详情、包裹明细和物流轨迹时间线。
- 待付款运单支持移动端余额支付弹窗，展示应付金额、当前余额和最近钱包流水。
- 已发货运单支持固定底部确认收货按钮。
- 寄件首页新增运单中心入口；申请打包按钮进入真实打包申请页；包裹详情中的申请打包带 `parcel_id` 跳转。

## 验证

- `pnpm --filter @crossborder-erp/mobile-h5 lint`
- `pnpm --filter @crossborder-erp/mobile-h5 build`
- `pnpm lint`
- `pnpm build`
- `cd backend && uv run pytest`
- `cd backend && uv run python manage.py check`
- `cd backend && uv run python manage.py makemigrations --check --dry-run`
- `actionlint .github/workflows/ci.yml`
- 移动浏览器联动验收：
  - 375px 视口下通过 `/ship/packing?parcel_id=7` 选择 `P00000007` 创建 `W00000004`。
  - 后台审核、设置费用 `13.40` 并充值后，移动端余额支付成功，钱包余额回读为 `32.00`。
  - 后台发货后，移动端可见轨迹 `FEM-001C 已从深圳仓发出`。
  - 移动端确认收货后，`W00000004` 进入 `SIGNED`，后端回读为 1 个包裹和 2 条轨迹。
  - 375px 视口下确认收货按钮与底部五栏不重叠，按钮中心命中自身。

## 边界

- 不接真实支付 SDK，本轮只验证钱包余额支付。
- 计费、充值、发货仍由后台承接，移动端不越权直接修改后台状态。
- PostgreSQL/MySQL/Redis/Docker 仍不在本轮真实验证范围内。
