# 2026-05-09 FEU-001C

## 任务

用户 Web 申请打包、运单、支付、轨迹。

## 交付

- 新增用户端 `/waybills` 运单中心，覆盖在库可打包包裹选择、同仓校验、收件信息录入和 Waybill 创建。
- 运单列表支持状态筛选、关键字搜索、分页、详情展示和包裹明细展示。
- 待付款运单支持余额支付弹窗，展示应付金额、当前余额和最近钱包流水，并调用余额支付 API。
- 运单详情展示物流轨迹时间线，已发货运单支持用户确认收货。
- 用户控制台新增运单中心入口、余额展示和运单待处理统计；包裹详情中的“申请打包”进入真实运单创建页。

## 验证

- `pnpm --filter @crossborder-erp/user-web lint`
- `pnpm --filter @crossborder-erp/user-web build`
- `pnpm lint`
- `pnpm build`
- `cd backend && uv run pytest`
- `cd backend && uv run python manage.py check`
- `cd backend && uv run python manage.py makemigrations --check --dry-run`
- `actionlint .github/workflows/ci.yml`
- 浏览器联动验收：
  - 用户通过 `/waybills?parcel_id=6` 选择 `IN_STOCK` 包裹 `P00000006` 创建运单 `W00000003`。
  - 后台审核、设置费用 `12.30` 并人工充值后，用户 Web 余额支付成功，钱包余额从 `52.70` 变为 `40.40`。
  - 后台发货后，用户 Web 可查看轨迹 `FEU-001C 已从深圳仓发出`。
  - 用户 Web 确认收货后，`W00000003` 进入 `SIGNED`，后端回读为 1 个包裹和 2 条轨迹。

## 边界

- 不接真实支付网关，当前只验证余额支付。
- 发货、计费、充值仍由后台任务和后台页面承接，用户端不越权直接修改状态。
- PostgreSQL/MySQL/Redis/Docker 仍不在本轮真实验证范围内。
