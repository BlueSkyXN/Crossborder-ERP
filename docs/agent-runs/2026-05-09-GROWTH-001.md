# 2026-05-09 GROWTH-001 积分/推广/返利基础

输入来自 `docs/ai-dev-baseline/agent-execution/tasks/GROWTH-001.md`，目标是在会员域补齐积分流水、邀请关系和返利统计基础。继续遵守 no-Docker、SQLite-first、本地同步任务边界；本轮不接真实广告联盟、提现、税务、多级分销、自动结算或最终积分/返利商业规则。

本轮在 `backend/apps/members` 新增 `PointLedger`、`ReferralRelation`、`RebateRecord` 和相关枚举，并补迁移 `0003_referralrelation_rebaterecord_pointledger_and_more.py`。用户端 API 提供积分摘要、积分流水、邀请关系和返利记录读取；后台 API 提供会员增长审计、积分调整、邀请关系创建和返利记录登记。返利奖励积分会写入积分流水，但返利金额不写入钱包余额、不生成 `PaymentOrder`，与线下汇款、钱包流水和应付核销保持隔离。未定规则统一以 `TODO_CONFIRM` 进入备注和摘要字段。

三端入口已补齐：Admin Web 的会员详情抽屉增加“积分推广”审计区，可查看积分、邀请码、邀请关系、返利记录并手工调整积分；User Web dashboard 增加“积分推广”入口和摘要卡；Mobile H5 `/me` 增加积分推广面板。Browser Smoke 已覆盖 Admin `/members` 详情抽屉、User `/dashboard` 和 Mobile `/me` 的增长入口文案。

验证已完成：`uv run python manage.py check`、`uv run python manage.py makemigrations --check --dry-run`、`uv run python manage.py spectacular --file /tmp/crossborder-growth-openapi.yaml --validate`、`uv run pytest apps/members/tests/test_growth.py -q`、`uv run pytest`、`npm run e2e`、`npm run e2e:browser`、`pnpm lint`、`pnpm build`、`git diff --check`、`actionlint .github/workflows/ci.yml`、Ruby YAML parse。`npm run e2e` 已打印 `[GROWTH-001] 积分推广返利链路完成`；Browser Smoke 使用 `.tmp/browser-e2e/chrome-profile` 临时 profile，不使用用户日常 Chrome profile，并在本轮最终验证后清理完成。

未验证边界：积分兑换、自动发放规则、邀请归因风控、返利比例、结算周期、提现、税务、真实联盟渠道、多级分销、PostgreSQL/MySQL/Redis/Celery 并发行为均未纳入本轮。任务图当前已执行到 `GROWTH-001`，后续如继续收敛生产级差距，建议单独确认 Excel 原生解析、完整浏览器旅程、审计日志或生产化边界任务。
