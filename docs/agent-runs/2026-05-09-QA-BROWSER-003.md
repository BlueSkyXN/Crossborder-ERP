# QA-BROWSER-003 Browser Smoke 稳定性加固

## 输入

- 用户总目标要求 CI 齐全、避免环境污染，并继续补齐剩余缺口。
- PR #49 合并后，`main` run `25597389515` 首次 Browser Smoke 在 Admin Web 登录页等待 `管理员登录` 时偶发失败；重跑 failed jobs 后同一 merge commit 通过。
- 任务卡：`docs/ai-dev-baseline/agent-execution/tasks/QA-BROWSER-003.md`。

## Agent 决策

- 不引入 Playwright/Vitest，不下载浏览器，继续复用现有 system Chrome CDP smoke。
- 直接加固 CDP 导航等待：先确认目标 URL、再确认 document ready，再断言页面文本；失败时重试一次并输出页面快照。
- Shell 清理前打印后端和三端 Vite 服务日志尾部，避免 CI 失败后 `.tmp/browser-e2e` 被删除导致无法定位。

## 修改

- `scripts/e2e/browser-smoke.mjs`：新增页面快照、URL 等待、document ready 等待、导航重试和文本等待失败诊断。
- `scripts/e2e/browser-smoke.sh`：失败清理前输出服务日志尾部。
- 更新 README、差距地图、生产 backlog、已知问题、任务图和 current-state。

## 验证

- `node --check scripts/e2e/browser-smoke.mjs`：通过。
- `bash -n scripts/e2e/browser-smoke.sh`：通过。
- `npm run e2e:browser`：通过，覆盖临时 SQLite/media/Chrome profile、三端 smoke、真实包裹预报/扫描入库/会员回看在库旅程。
- `npm run e2e`：通过，1 passed。
- `cd backend && uv run pytest`：150 passed。
- `pnpm lint`：通过。
- `pnpm build`：通过。
- `git diff --check`：通过。
- `actionlint .github/workflows/ci.yml`：通过。
- YAML parse：`yaml ok`，仅出现外置盘 `/Volumes/TP4000PRO` world-writable PATH warning。

## 未验证边界

- 仍不声明完成视觉回归、组件级测试或全业务浏览器覆盖。
- PostgreSQL/MySQL/Redis/Celery/Docker 仍不真实验证。
